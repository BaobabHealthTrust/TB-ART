<% if @source.blank?%>
  <script>
    var tt_cancel_destination = "/patients/show/" + "<%=@patient_id%>"
  </script>
<%else%>
    <script>
      var tt_cancel_destination = "<%= "/cohort_tool/#{@source}?quarter=#{@quarter}&report_type=#{@source}&arv_end_number=#{@arv_end_number}&arv_start_number=#{@arv_start_number}"-%>";
    </script>
<%end%>
  
<%= render :partial => "mastercard_demographics" %>
<%= render :partial => "mastercard_visits" %>
